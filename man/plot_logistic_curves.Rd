% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_logistic_curves.R
\name{plot_logistic_curves}
\alias{plot_logistic_curves}
\title{Response curve plots for a bootstrapped logistic regression}
\usage{
plot_logistic_curves(
  bl,
  n_grid = 200L,
  point_size = 3,
  line_size = 0.9,
  ribbon_alpha = 0.2,
  error_width = 0.15,
  raw_data = TRUE,
  raw_point_size = 1.5,
  raw_alpha = 0.25,
  jitter_height = 0.02,
  obs_groups = 4L,
  obs_colour = "#333333",
  obs_size = 3,
  obs_error_width = 0.25,
  colour_by = NULL,
  obs_exclude = NULL,
  obs_by_group = FALSE,
  colour_by_label = NULL,
  group_palette = NULL,
  colour_by_dodge = 0.015,
  palette = c("#2C7BB6", "#D7191C", "#1A9641", "#F46D43", "#756BB1", "#FDAE61"),
  base_theme = NULL,
  free_y = FALSE,
  combine = FALSE
)
}
\arguments{
\item{bl}{A \code{boot_logistic} object returned by \code{\link[=bootstrap_logistic]{bootstrap_logistic()}}.}

\item{n_grid}{Integer; number of equally-spaced grid points for continuous
predictor curves. Default \code{200L}.}

\item{point_size}{Numeric; size of the predicted-probability marker in
categorical plots. Default \code{3}.}

\item{line_size}{Numeric; line width of the probability curve in continuous
plots. Default \code{0.9}.}

\item{ribbon_alpha}{Numeric in \verb{(0, 1)}; transparency of the CI ribbon.
Default \code{0.20}.}

\item{error_width}{Numeric; cap width of CI error bars for model-predicted
probabilities in categorical plots. Default \code{0.15}.}

\item{raw_data}{Logical; overlay observed binary outcomes as jittered points.
Default \code{TRUE}.}

\item{raw_point_size}{Numeric; size of raw-data jitter points. Default \code{1.5}.}

\item{raw_alpha}{Numeric in \verb{(0, 1)}; transparency of raw-data jitter points.
Default \code{0.25}.}

\item{jitter_height}{Numeric; vertical jitter height for raw-data points.
Default \code{0.02}.}

\item{obs_groups}{Integer in \verb{[2, 10]} or \code{NULL}. Number of quantile strata
for the observed-proportion overlay on continuous predictors (2 = halves,
3 = tertiles, 4 = quartiles). Each category level is its own stratum for
factor predictors regardless of this value. \code{NULL} suppresses the overlay.
Default \code{4L}.}

\item{obs_colour}{Colour for the observed-proportion overlay squares and
error bars. Always used when \code{obs_by_group = FALSE} (the default), and
ignored when \code{obs_by_group = TRUE}. Default \code{"#333333"}.}

\item{obs_size}{Numeric; size of the observed-proportion marker squares.
Default \code{3}.}

\item{obs_error_width}{Numeric; cap width of observed-proportion CI error
bars. Default \code{0.25}.}

\item{colour_by}{Character string naming a column in \code{bl$data} by which to
colour-code the raw jitter points. The column need not appear in the model
formula. Coerced to a factor if not already. \code{NULL} (default) uses a
single colour for all jitter points.}

\item{obs_exclude}{Character vector of levels of the \code{colour_by} variable
that should appear in the observed-proportion overlay as a \strong{single}
unquantiled bin (i.e. one square and one set of error bars covering all
observations in that group), rather than being split into \code{obs_groups}
quantile strata.  This is useful for groups whose predictor values are
constant or near-constant (e.g. a placebo arm with concentration = 0).
Jitter points are never affected.  \code{obs_exclude} is ignored when
\code{colour_by = NULL}. Default \code{NULL} (all groups binned normally).}

\item{obs_by_group}{Logical; if \code{TRUE} \strong{and} \code{colour_by} is set, the
observed-proportion squares and their CI bars are also split and coloured
by group. On continuous plots the per-group squares are dodged horizontally
by \code{colour_by_dodge} to prevent overlap; on categorical plots
\code{position_dodge()} is used. Default \code{FALSE}.}

\item{colour_by_label}{Character string used as the legend title when
\code{colour_by} is set. Defaults to the value of \code{colour_by}.}

\item{group_palette}{Character vector of colours for the grouping variable
levels, recycled as needed. When \code{NULL} (default) a built-in qualitative
palette is used.}

\item{colour_by_dodge}{Positive numeric; horizontal dodge distance for the
per-group observed-proportion markers on \strong{continuous} plots when
\code{obs_by_group = TRUE}, expressed as a fraction of the predictor's observed
range. Ignored when \code{obs_by_group = FALSE} or for categorical plots.
Default \code{0.015}.}

\item{palette}{Character vector of colours for the model curve and, for
categorical predictors, the predicted-probability fill. Recycled as needed.}

\item{base_theme}{A complete ggplot2 theme object applied to all plots.
Defaults to a \code{theme_bw}-based theme.}

\item{free_y}{Logical; if \code{FALSE} (default) all plots share a 0-1 y-axis.}

\item{combine}{Logical; if \code{TRUE} and \strong{patchwork} is installed, return a
single combined figure. Default \code{FALSE}.}
}
\value{
A named list of \code{ggplot} objects (one per predictor), or a
\code{patchwork} figure when \code{combine = TRUE}.
}
\description{
Generates one marginal response-curve plot per predictor term in a
\code{boot_logistic} model, using the bootstrap coefficient matrix to construct
pointwise confidence intervals. Non-focal predictors are held at their
\strong{median} (numeric) or \strong{mode} (factor / character).
}
\details{
Two plot geometries are used depending on predictor type:
\itemize{
\item \strong{Continuous predictors}: a probability curve with a shaded bootstrap CI
ribbon. Observed binary outcomes (0 / 1) are overlaid as jittered points
at their true y-value when \code{raw_data = TRUE}. When \code{obs_groups} is not
\code{NULL}, the empirical response rate per quantile stratum is overlaid as
filled squares with Wilson score CI error bars.
\item \strong{Categorical predictors}: predicted probability as a filled dot with
bootstrap CI error bars per level. Observed 0 / 1 outcomes are overlaid
as jittered points when \code{raw_data = TRUE}. When \code{obs_groups} is not
\code{NULL}, the empirical response rate per category level is also overlaid.
}

\strong{Colour-coding by a grouping variable} (\code{colour_by}): when a column name
is supplied, the raw jitter points are coloured by group level and a legend
is added automatically. The observed-proportion squares and their CI bars are
\strong{not} affected by \code{colour_by} by default; set \code{obs_by_group = TRUE} to
also split and colour the proportion overlay by group. The model curve and
ribbon are always drawn in a single colour.

\code{colour_by} may reference any column in \code{bl$data}, including columns that
do not appear in the model formula (e.g. a stratification variable).

If the formula contains inline transformations such as \code{log(age)}, plots are
produced on the \emph{original} (untransformed) scale; \code{model.matrix()} applies
the transformation internally when computing predictions.
}
\examples{
df <- data.frame(
  y   = rbinom(200, 1, 0.4),
  age = rnorm(200, 50, 10),
  sex = factor(sample(c("M", "F"), 200, replace = TRUE))
)
fit <- glm(y ~ age + sex, data = df, family = binomial)
bl  <- bootstrap_logistic(fit, n_boot = 200, seed = 1)

# Colour jitter points by sex; obs overlay stays single-colour
plots <- plot_logistic_curves(bl, colour_by = "sex")
plots[["age"]]

# Also split the obs proportion overlay by sex
plots2 <- plot_logistic_curves(bl, colour_by = "sex", obs_by_group = TRUE)
plots2[["age"]]

}
\seealso{
\code{\link[=bootstrap_logistic]{bootstrap_logistic()}}
}
