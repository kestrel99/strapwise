% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_logistic_emax.R
\name{predict.logistic_emax}
\alias{predict.logistic_emax}
\title{Predict from a \code{logistic_emax} object}
\usage{
\method{predict}{logistic_emax}(
  object,
  newdata = NULL,
  newlinear = NULL,
  newemax = NULL,
  newec50 = NULL,
  type = c("link", "response", "ci"),
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{logistic_emax} object.}

\item{newdata}{Optional numeric vector of x values at which to predict.
\code{NULL} (default) predicts at the original observed values.}

\item{newlinear}{Optional matrix of linear covariates at the prediction
points. Must have \code{length(newdata)} rows and the same number of columns as
the training \code{linear_covs}. When \code{NULL} and \code{newdata} is supplied, training
column means are used (with a message).}

\item{newemax}{Optional matrix of Emax covariates at the prediction points.
Same convention as \code{newlinear}.}

\item{newec50}{Optional matrix of EC50 covariates at the prediction points.
Same convention as \code{newlinear}.}

\item{type}{One of \code{"link"}, \code{"response"}, or \code{"ci"}. Default \code{"link"}.}

\item{level}{Confidence level for \code{type = "ci"}. Default \code{0.95}.}

\item{...}{Currently unused; included for S3 compatibility.}
}
\value{
For \code{type = "link"} or \code{"response"}, a numeric vector. For
\code{type = "ci"}, a data frame with columns \code{x}, \code{fit}, \code{lwr}, \code{upr}.
}
\description{
Generates predictions on the link, response, or confidence-interval scale.
For models with covariates, new covariate matrices can be supplied via
\code{newlinear}, \code{newemax}, and \code{newec50}; when these are \code{NULL} and a new \code{x}
vector is given, covariates are fixed at their training-data column means.
}
\details{
Confidence intervals (\code{type = "ci"}) use the delta method on the link scale,
propagating uncertainty through all model parameters including covariate
effects.
}
\examples{
set.seed(1)
dose <- sort(rep(c(0, 5, 10, 25, 50), length.out = 150))
eta  <- qlogis(0.10) + 2 * dose / (10 + dose)
y    <- rbinom(150, 1, plogis(eta))
fit  <- fit_logistic_emax(y, dose)

grid <- seq(0, 50, length.out = 100)
head(predict(fit, newdata = grid, type = "ci"))

}
\seealso{
\code{\link[=fit_logistic_emax]{fit_logistic_emax()}}
}
