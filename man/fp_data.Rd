% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_plot.R
\name{fp_data}
\alias{fp_data}
\title{Build a tidy data frame for a forest plot}
\usage{
fp_data(
  model,
  data,
  labels = NULL,
  log_vars = NULL,
  percs = c(0.05, 0.25, 0.75, 0.95),
  ci_level = 0.95,
  digits = 3L,
  ci_width = 34L
)
}
\arguments{
\item{model}{A fitted \code{glm} (binomial family) or \code{coxph} object.}

\item{data}{Data frame used to fit \code{model}.}

\item{labels}{Named character vector mapping raw variable names to
publication-ready display labels.  Unmatched names fall back to the
raw variable name.  Interaction term names (e.g. \code{"age:bmi"}) can also
be supplied as keys.}

\item{log_vars}{Character vector of variable names stored on the log scale.
Their percentile display values are back-transformed with \code{exp()} so
readers see the original units.}

\item{percs}{Numeric vector of percentiles shown for continuous
predictors (default \code{c(0.05, 0.25, 0.75, 0.95)}).}

\item{ci_level}{Confidence level for the CI text column (default \code{0.95}).}

\item{digits}{Significant figures used when formatting percentile display
values and the estimate/CI text column (default \code{3}).}

\item{ci_width}{Integer; number of spaces in the CI-panel spacer column.
Increase to widen the CI drawing area in \code{\link[=fp_plot]{fp_plot()}} (default \code{34}).}
}
\value{
A data frame with one row per displayed entry containing:
\code{Predictor}, \code{est}, \code{lci}, \code{uci}, \code{pval}, \code{is_header}, \code{is_reference},
\code{group}, the model-specific estimate column (\verb{OR (95\% CI)} or
\verb{HR (95\% CI)}), \code{P-value}, and \code{" "} (spacer column).
}
\description{
Prepares a structured data frame from a fitted binomial \code{glm} or \code{coxph}
object suitable for passing to \code{\link[=fp_plot]{fp_plot()}}.  The model type is
auto-detected from the object class.
}
\details{
For categorical predictors one row is produced per factor level, with the
first level as the reference category.  The p-value on the header row is
derived from the Wald statistic of the first non-reference contrast
(appropriate for binary predictors; compute a likelihood-ratio p-value
separately for predictors with more than two levels).

For continuous predictors each row represents an estimated OR/HR relative
to the predictor's median in \code{data}.

Interaction terms (those containing \code{":"}) are detected automatically.  A
continuous x continuous interaction is shown as a single row.  An
interaction involving a factor is shown as a header row with one child row
per factor level present in the model.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  lung <- survival::lung
  lung$status <- lung$status - 1L
  lung$sex <- factor(lung$sex, levels = 1:2, labels = c("Male", "Female"))
  lung_cc <- lung[
    complete.cases(lung[, c("time", "status", "age", "sex")]), ]

  fit <- survival::coxph(
    survival::Surv(time, status) ~ age + sex,
    data = lung_cc
  )
  pd <- fp_data(fit, lung_cc,
                labels = c(age = "Age (years)", sex = "Sex"))
  head(pd[, c("Predictor", "HR (95\% CI)", "P-value")])
}

}
\seealso{
\code{\link[=fp_plot]{fp_plot()}}
}
