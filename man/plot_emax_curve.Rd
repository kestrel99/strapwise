% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_emax_curve.R
\name{plot_emax_curve}
\alias{plot_emax_curve}
\title{Response curve plot for a bootstrapped logistic Emax model}
\usage{
plot_emax_curve(
  be,
  n_grid = 200L,
  x_range = NULL,
  newlinear = NULL,
  newemax = NULL,
  newec50 = NULL,
  line_size = 0.9,
  ribbon_alpha = 0.2,
  raw_data = TRUE,
  raw_point_size = 1.5,
  raw_alpha = 0.25,
  jitter_height = 0.02,
  obs_groups = 4L,
  obs_colour = "#333333",
  obs_size = 3,
  obs_error_width = 0.25,
  palette = c("#2C7BB6", "#D7191C", "#1A9641", "#F46D43", "#756BB1", "#FDAE61"),
  base_theme = NULL,
  free_y = FALSE,
  x_label = "x",
  y_label = "P(Response = 1)"
)
}
\arguments{
\item{be}{A \code{boot_emax} object returned by \code{\link[=bootstrap_emax]{bootstrap_emax()}}.}

\item{n_grid}{Integer; number of equally-spaced grid points for the fitted
curve. Default \code{200L}.}

\item{x_range}{Numeric vector \code{c(lo, hi)} defining the x-axis range.
Defaults to the observed range in the training data.}

\item{newlinear}{Optional numeric matrix with \code{n_grid} rows supplying linear
covariate values for the prediction grid. Must have the same number of
columns as the training \code{linear_covs}. \code{NULL} (default) uses training
column means.}

\item{newemax}{Optional numeric matrix of Emax covariate values for the
prediction grid. Same convention as \code{newlinear}.}

\item{newec50}{Optional numeric matrix of EC50 covariate values for the
prediction grid. Same convention as \code{newlinear}.}

\item{line_size}{Numeric; probability curve line width. Default \code{0.9}.}

\item{ribbon_alpha}{Numeric in \verb{(0, 1)}; CI ribbon transparency. Default
\code{0.20}.}

\item{raw_data}{Logical; overlay observed 0/1 outcomes as jittered points.
Default \code{TRUE}.}

\item{raw_point_size}{Numeric; jitter point size. Default \code{1.5}.}

\item{raw_alpha}{Numeric in \verb{(0, 1)}; jitter point transparency. Default
\code{0.25}.}

\item{jitter_height}{Numeric; vertical jitter height. Default \code{0.02}.}

\item{obs_groups}{Integer in \verb{[2, 10]} or \code{NULL}; number of quantile bins
for the observed-proportion overlay (squares with Wilson score CI bars).
\code{NULL} suppresses the overlay. Default \code{4L}.}

\item{obs_colour}{Colour for the observed-proportion markers and error bars.
Default \code{"#333333"}.}

\item{obs_size}{Numeric; size of the observed-proportion squares. Default
\code{3}.}

\item{obs_error_width}{Numeric; cap width of observed-proportion CI error
bars. Default \code{0.25}.}

\item{palette}{Character vector of colours; the first element is used for
the curve, ribbon, and raw jitter points.}

\item{base_theme}{A complete ggplot2 theme object applied to the plot.
Defaults to a \code{theme_bw}-based theme.}

\item{free_y}{Logical; if \code{FALSE} (default) the y-axis spans 0-1.}

\item{x_label}{Character; x-axis label. Default \code{"x"}.}

\item{y_label}{Character; y-axis label. Default \code{"P(Response = 1)"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a response probability curve with a bootstrap percentile CI ribbon
and optional overlays of observed binary outcomes and binned observed
proportions from a \code{boot_emax} object.
}
\details{
Other covariates (linear, Emax modifier, and EC50 modifier) are fixed at
their training-data column means for the fitted curve unless custom matrices
are supplied via \code{newlinear}, \code{newemax}, or \code{newec50}. When such covariates
are present the subtitle reports what values were used.

Bootstrap CIs are percentile intervals: each replicate in \code{be$boot_theta}
is used to compute a response curve, and the \code{alpha/2} and
\code{1 - alpha/2} quantiles across replicates form the ribbon bounds.
}
\examples{
set.seed(42)
n    <- 200
dose <- sort(rep(c(0, 5, 10, 25, 50, 100), length.out = n))
eta  <- qlogis(0.10) + (qlogis(0.75) - qlogis(0.10)) * dose / (10 + dose)
y    <- rbinom(n, 1, plogis(eta))
fit  <- fit_logistic_emax(y, dose)
be   <- bootstrap_emax(fit, n_boot = 200, seed = 1)

plot_emax_curve(be)

}
\seealso{
\code{\link[=bootstrap_emax]{bootstrap_emax()}}, \code{\link[=fit_logistic_emax]{fit_logistic_emax()}}
}
