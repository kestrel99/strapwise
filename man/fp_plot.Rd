% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_plot.R
\name{fp_plot}
\alias{fp_plot}
\title{Build and render a forest plot from a fitted model}
\usage{
fp_plot(
  model,
  data,
  labels = NULL,
  log_vars = NULL,
  percs = c(0.05, 0.25, 0.75, 0.95),
  ci_level = 0.95,
  digits = 3L,
  ci_width = 34L,
  xlim = c(0.1, 10),
  vert_line = c(0.8, 1.25),
  ci_col = "#C00000",
  base_size = 10,
  x_trans = "log",
  ticks_at = NULL
)
}
\arguments{
\item{model}{A fitted \code{glm} (binomial family) or \code{coxph} object.}

\item{data}{Data frame used to fit \code{model}.}

\item{labels}{Named character vector; display labels for predictor names
(passed to \code{\link[=fp_data]{fp_data()}}).}

\item{log_vars}{Character vector of variable names stored on the log scale
(passed to \code{\link[=fp_data]{fp_data()}}).}

\item{percs}{Percentiles shown for continuous predictors
(passed to \code{\link[=fp_data]{fp_data()}}, default \code{c(0.05, 0.25, 0.75, 0.95)}).}

\item{ci_level}{Confidence level (passed to \code{\link[=fp_data]{fp_data()}}, default \code{0.95}).}

\item{digits}{Significant figures for formatted text
(passed to \code{\link[=fp_data]{fp_data()}}, default \code{3}).}

\item{ci_width}{Spacer column width (passed to \code{\link[=fp_data]{fp_data()}}, default \code{34}).}

\item{xlim}{Length-2 numeric; x-axis limits on the original
(non-transformed) scale (default \code{c(0.1, 10)}).}

\item{vert_line}{Numeric vector of additional vertical reference lines
(default \code{c(0.8, 1.25)}).  Set to \code{NULL} to suppress.}

\item{ci_col}{Colour string for CI points and bars (default dark red).}

\item{base_size}{Base font size (default \code{10}).}

\item{x_trans}{Axis transformation: \code{"log"} (default), \code{"log2"},
\code{"log10"}, or \code{"none"}.}

\item{ticks_at}{Numeric vector of custom tick positions on the original
x-axis scale.  \code{NULL} (default) uses automatic ticks.}
}
\value{
A \code{gtable} object of class \code{"forestplot"}.  Print it directly or
draw it with \code{grid::grid.draw()} inside a graphics device.
}
\description{
Calls \code{\link[=fp_data]{fp_data()}} to prepare the plot data frame and then renders a
\code{forestploter} forest plot in one step.  All \code{\link[=fp_data]{fp_data()}} arguments are
forwarded directly; call \code{\link[=fp_data]{fp_data()}} separately first only if you need to
inspect or modify the intermediate data frame before plotting.
}
\details{
The model type (logistic or Cox) is auto-detected from the object class.

The four display columns are:
\enumerate{
\item \code{Predictor}  -- row labels
\item \verb{OR (95\% CI)} or \verb{HR (95\% CI)}  -- formatted estimate string
\item \code{" "}  -- spacer column where the CI glyph is drawn
\item \code{P-value}
}

Header rows (one per predictor) receive a shaded background and bold label.
P-values below 0.05 are coloured red.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  lung <- survival::lung
  lung$status <- lung$status - 1L
  lung$sex <- factor(lung$sex, levels = 1:2, labels = c("Male", "Female"))
  lung_cc <- lung[
    complete.cases(lung[, c("time", "status", "age", "sex")]), ]

  fit <- survival::coxph(
    survival::Surv(time, status) ~ age + sex,
    data = lung_cc
  )
  fp_plot(fit, lung_cc,
          labels = c(age = "Age (years)", sex = "Sex"))
}

}
\seealso{
\code{\link[=fp_data]{fp_data()}}
}
