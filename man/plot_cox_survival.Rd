% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cox.R
\name{plot_cox_survival}
\alias{plot_cox_survival}
\title{Adjusted survival curves for a bootstrapped Cox model}
\usage{
plot_cox_survival(
  bc,
  newdata,
  curve_labels = NULL,
  times = NULL,
  fun = c("survival", "event", "cumhaz"),
  ribbon_alpha = 0.15,
  line_size = 0.9,
  palette = c("#2C7BB6", "#D7191C", "#1A9641", "#F46D43", "#756BB1", "#FDAE61"),
  base_theme = NULL,
  x_label = "Time",
  y_label = NULL
)
}
\arguments{
\item{bc}{A \code{boot_cox} object returned by \code{\link[=bootstrap_cox]{bootstrap_cox()}}.}

\item{newdata}{A data frame where each row defines covariate values for one
predicted curve. Must contain all predictor columns used in the model.}

\item{curve_labels}{Optional character vector of labels for each row of
\code{newdata}. Defaults to \code{"Curve 1"}, \code{"Curve 2"}, etc.}

\item{times}{Optional numeric vector of time points at which to evaluate the
curves. \code{NULL} (default) uses all event times from the original data.}

\item{fun}{One of \code{"survival"} (default), \code{"event"} (1 - S(t)), or
\code{"cumhaz"} (-log S(t)).}

\item{ribbon_alpha}{Numeric in \verb{(0, 1)}; CI ribbon transparency. Default
\code{0.15}.}

\item{line_size}{Numeric; curve line width. Default \code{0.9}.}

\item{palette}{Character vector of colours, one per curve (recycled as
needed).}

\item{base_theme}{A complete ggplot2 theme object. Defaults to a
\code{theme_bw}-based theme.}

\item{x_label}{Character; x-axis label. Default \code{"Time"}.}

\item{y_label}{Character; y-axis label. \code{NULL} (default) is set
automatically from \code{fun}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots model-predicted survival (or event probability, or cumulative hazard)
over time for each row of \code{newdata}, with bootstrap percentile confidence
ribbons.
}
\details{
Confidence bands are computed by fixing the baseline cumulative hazard at the
original-fit estimate and varying only the linear predictor across bootstrap
replicates:
\deqn{S_b(t \mid x) = \exp\!\bigl(-\hat{H}_0(t)\cdot e^{x^\top\beta_b}\bigr)}
where \eqn{\hat{H}_0} is from \code{\link[survival:basehaz]{survival::basehaz()}} and \eqn{\beta_b} is the
\eqn{b}-th bootstrap coefficient vector. This correctly reflects uncertainty
in the covariate effects; uncertainty in the baseline hazard is not
propagated.

Stratified Cox models (those using \code{strata()} in the formula) are not
currently supported; an error is raised if strata are detected.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  lung <- survival::lung
  lung$status <- lung$status - 1L
  fit <- survival::coxph(
    survival::Surv(time, status) ~ age + sex + ph.ecog,
    data = lung
  )
  bc <- bootstrap_cox(fit, n_boot = 200, seed = 1)

  nd <- data.frame(
    age    = c(50, 70),
    sex    = c(1,  1),
    ph.ecog = c(0, 2)
  )
  plot_cox_survival(bc, newdata = nd,
                    curve_labels = c("Age 50, ECOG 0", "Age 70, ECOG 2"))
}

}
\seealso{
\code{\link[=bootstrap_cox]{bootstrap_cox()}}, \code{\link[=plot_cox_forest]{plot_cox_forest()}}
}
