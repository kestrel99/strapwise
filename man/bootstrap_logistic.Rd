% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_logistic.R
\name{bootstrap_logistic}
\alias{bootstrap_logistic}
\title{Bootstrap a fitted binomial GLM}
\usage{
bootstrap_logistic(fit, n_boot = 1000L, seed = 42L, conf_level = 0.95)
}
\arguments{
\item{fit}{A fitted \code{\link[=glm]{glm()}} object with \code{family = binomial} (any link).}

\item{n_boot}{Number of bootstrap replicates. Default \code{1000L}.}

\item{seed}{Integer random seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Default \code{42L}.}

\item{conf_level}{Confidence level for percentile intervals, strictly
between 0 and 1. Default \code{0.95}.}
}
\value{
An S3 object of class \code{"boot_logistic"}, a named list with elements:
\describe{
\item{\code{fit}}{The original \code{glm} object.}
\item{\code{boot_coefs}}{Numeric matrix of bootstrapped coefficients from
successful refits (\code{n_success} x \code{p}), after removing skipped
replicates and rows with any \code{NA} coefficient.}
\item{\code{data}}{The original (untransformed) data frame recovered from
the fit call.}
\item{\code{formula}}{The model formula.}
\item{\code{conf_level}}{The requested confidence level.}
\item{\code{n_boot}}{Number of successful bootstrap replicates retained.}
}
}
\description{
Runs case-resampling bootstrap on a fitted \code{glm} object with
\code{family = binomial}, returning a matrix of bootstrapped coefficients
alongside the original fit. The result is consumed by
\code{\link[=plot_logistic_curves]{plot_logistic_curves()}} to construct pointwise confidence bands, and
can also be inspected directly via \code{\link[=print.boot_logistic]{print.boot_logistic()}} and
\code{\link[=summary.boot_logistic]{summary.boot_logistic()}}.
}
\details{
The function recovers the original (untransformed) data from \code{fit$call$data}
so that \code{model.matrix()} can apply any inline formula transformations
(e.g. \code{log(age)}) itself during bootstrap refitting. If the call environment
cannot be evaluated --for example when the fit was constructed inside another
function --it falls back to \code{model.frame(fit)} with a warning; in that case
formulae containing inline transformations may not plot correctly.

Bootstrap replicates that error or generate warnings during refitting are
skipped and counted. If any replicates are skipped, a message is emitted.
Replicates with any \code{NA} coefficient (e.g. arising from complete separation
in a resample) are also dropped before the result is returned.
}
\examples{
df <- data.frame(
  y   = rbinom(200, 1, 0.4),
  age = rnorm(200, 50, 10),
  sex = factor(sample(c("M", "F"), 200, replace = TRUE))
)
fit <- glm(y ~ age + sex, data = df, family = binomial)
bl  <- bootstrap_logistic(fit, n_boot = 200, seed = 1)
print(bl)
summary(bl)

}
\seealso{
\code{\link[=plot_logistic_curves]{plot_logistic_curves()}}, \code{\link[=summary.boot_logistic]{summary.boot_logistic()}}
}
