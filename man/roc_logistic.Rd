% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc_logistic.R
\name{roc_logistic}
\alias{roc_logistic}
\title{Overlaid ROC curves for one or more fitted models}
\usage{
roc_logistic(
  models,
  outcome,
  labels = NULL,
  thresholds = NULL,
  ci = TRUE,
  ref_line = TRUE,
  fill = NULL,
  alpha = 0.2,
  colors = NULL,
  linewidth = 1.1,
  title = "ROC Curves",
  ...
)
}
\arguments{
\item{models}{A named list of one or more fitted model objects.}

\item{outcome}{Numeric binary (0/1) vector of observed outcomes, the same
length as the fitted values returned by each model.}

\item{labels}{Optional character vector of display labels, one per model.
Defaults to \code{names(models)}; unnamed models are labelled \code{"Model 1"},
\code{"Model 2"}, etc.}

\item{thresholds}{Numeric vector of classification thresholds. When \code{NULL}
(default), 501 evenly spaced values from 1 to 0 are used.}

\item{ci}{Logical; if \code{TRUE} (default) a 95\% Hanley-McNeil confidence
interval is appended to each legend entry.}

\item{ref_line}{Logical; if \code{TRUE} (default) a dashed diagonal chance line
is drawn.}

\item{fill}{Logical; whether to shade the area under the ROC curve. When
\code{NULL} (default), shading is applied for a single model and suppressed for
multiple models.}

\item{alpha}{Opacity of the AUC shading (single-model case). Default \code{0.2}.}

\item{colors}{Optional character vector of line colours, one per model.
Defaults to the ggplot2 discrete colour scale.}

\item{linewidth}{Line width. Default \code{1.1}.}

\item{title}{Plot title. Default \code{"ROC Curves"}.}

\item{...}{Currently unused; included for S3 compatibility.}
}
\value{
An S3 object of class \code{"roc_logistic"}, a named list with:
\describe{
\item{\code{plot}}{A \code{ggplot} object.}
\item{\code{roc_list}}{Named list of ROC data frames (one per model), each
with columns \code{threshold}, \code{sensitivity}, \code{specificity}, \code{fpr}, and
\code{youden}.}
\item{\code{auc}}{Named numeric vector of AUC values.}
\item{\code{best_threshold}}{Named list of single-row data frames for the
Youden-optimal threshold of each model.}
}
}
\description{
Computes ROC curves for one or more fitted models using the same trapezoidal
AUC and Youden-threshold logic as \code{\link[=roc_logistic_emax]{roc_logistic_emax()}}, overlays them on a
single panel, and returns ROC data, AUC values, and the ggplot object.
}
\details{
Any model class is supported provided \code{predict(object, type = "response")}
returns a numeric vector of fitted probabilities the same length as
\code{outcome}.
}
\examples{
set.seed(1)
n   <- 300
age <- rnorm(n, 55, 10)
bmi <- rnorm(n, 27, 5)
y   <- rbinom(n, 1, plogis(-4 + 0.05 * age + 0.06 * bmi))
df  <- data.frame(y = y, age = age, bmi = bmi)

m1 <- glm(y ~ age,       data = df, family = binomial)
m2 <- glm(y ~ age + bmi, data = df, family = binomial)

roc <- roc_logistic(list(Age = m1, Full = m2), outcome = y)
print(roc)

}
\seealso{
\code{\link[=roc_logistic_emax]{roc_logistic_emax()}}, \code{\link[=compare_logistic_roc]{compare_logistic_roc()}}
}
