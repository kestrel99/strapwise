% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cox.R
\name{plot_cox_forest}
\alias{plot_cox_forest}
\title{Forest plot of hazard ratios for a bootstrapped Cox model}
\usage{
plot_cox_forest(
  bc,
  log_scale = TRUE,
  point_size = 3,
  line_size = 0.7,
  ref_line = TRUE,
  palette = c("#2C7BB6", "#D7191C", "#1A9641", "#F46D43", "#756BB1", "#FDAE61"),
  base_theme = NULL
)
}
\arguments{
\item{bc}{A \code{boot_cox} object returned by \code{\link[=bootstrap_cox]{bootstrap_cox()}}.}

\item{log_scale}{Logical; if \code{TRUE} (default) the x-axis is on the log scale
so that symmetric CI bars reflect log-HR symmetry. Set to \code{FALSE} for a
linear HR scale.}

\item{point_size}{Numeric; size of the HR point estimate marker. Default \code{3}.}

\item{line_size}{Numeric; line width of the CI bars. Default \code{0.7}.}

\item{ref_line}{Logical; draw a vertical reference line at HR = 1. Default
\code{TRUE}.}

\item{palette}{Character vector of colours; the first element is used for all
points and CI bars.}

\item{base_theme}{A complete ggplot2 theme object. Defaults to a
\code{theme_bw}-based theme.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots each coefficient as a point estimate (hazard ratio) with a horizontal
bootstrap percentile confidence interval bar, one row per term.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  lung <- survival::lung
  lung$status <- lung$status - 1L
  fit <- survival::coxph(
    survival::Surv(time, status) ~ age + sex + ph.ecog,
    data = lung
  )
  bc <- bootstrap_cox(fit, n_boot = 200, seed = 1)
  plot_cox_forest(bc)
}

}
\seealso{
\code{\link[=bootstrap_cox]{bootstrap_cox()}}, \code{\link[=plot_cox_survival]{plot_cox_survival()}}
}
