% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_logistic_roc.R
\name{compare_logistic_roc}
\alias{compare_logistic_roc}
\title{Compare logistic regression models with overlaid ROC curves}
\usage{
compare_logistic_roc(
  models,
  model_names = names(models),
  n_cuts = 0L,
  labels = FALSE,
  show_auc = TRUE,
  auc_digits = 3L,
  diagonal = TRUE,
  legend_title = "Model",
  base_theme = NULL
)
}
\arguments{
\item{models}{A list of at least two fitted \code{\link[=glm]{glm()}} objects with
\code{family = binomial}.}

\item{model_names}{Optional character vector of display names for \code{models}.
Defaults to \code{names(models)} when present, otherwise \code{"Model 1"},
\code{"Model 2"}, and so on.}

\item{n_cuts}{Integer; number of cutoff labels drawn along each ROC curve.
Default \code{0L} for clean overlaid curves.}

\item{labels}{Logical; whether to draw cutoff labels. Default \code{FALSE}.}

\item{show_auc}{Logical; if \code{TRUE} (default), append AUC values to the legend
labels.}

\item{auc_digits}{Integer; number of decimal places used when formatting AUC
values in the legend. Default \code{3L}.}

\item{diagonal}{Logical; if \code{TRUE} (default), add a dashed 45-degree
reference line.}

\item{legend_title}{Character string for the colour legend title. Default
\code{"Model"}.}

\item{base_theme}{Optional complete ggplot2 theme object added after
\code{\link[plotROC:style_roc]{plotROC::style_roc()}}. Default \code{NULL}.}
}
\value{
A \code{ggplot} object containing one overlaid ROC curve per model.
}
\description{
Builds a single ROC plot that overlays the performance of multiple fitted
binomial \code{\link[=glm]{glm()}} models using \strong{ggplot2} and \strong{plotROC}.
}
\details{
Models are aligned on the intersection of their fitted observations, using
row names from each model frame. This allows comparison of models that were
fit on slightly different subsets because of missing predictors, as long as
they still share some observations in common and the binary outcome agrees on
those shared rows.
}
\examples{
df <- data.frame(
  y = rbinom(250, 1, 0.5),
  age = rnorm(250, 50, 10),
  bmi = rnorm(250, 27, 5),
  sex = factor(sample(c("F", "M"), 250, replace = TRUE))
)
df$y <- rbinom(
  250, 1,
  plogis(-4 + 0.05 * df$age + 0.06 * df$bmi + 0.5 * (df$sex == "M"))
)

fit_age <- glm(y ~ age, data = df, family = binomial)
fit_full <- glm(y ~ age + bmi + sex, data = df, family = binomial)

compare_logistic_roc(
  list("Age only" = fit_age, "Full model" = fit_full)
)

}
\seealso{
\code{\link[=plot_logistic_curves]{plot_logistic_curves()}}, \code{\link[=bootstrap_logistic]{bootstrap_logistic()}}
}
