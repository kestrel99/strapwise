% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_logistic_emax.R
\name{fit_logistic_emax}
\alias{fit_logistic_emax}
\title{Fit a logistic Emax model}
\usage{
fit_logistic_emax(
  y,
  x,
  linear_covs = NULL,
  emax_covs = NULL,
  ec50_covs = NULL,
  start = NULL,
  control = list(maxit = 1000),
  hessian = TRUE,
  ...
)
}
\arguments{
\item{y}{Numeric binary outcome vector (0 or 1).}

\item{x}{Numeric predictor vector (e.g. dose, concentration, time), same
length as \code{y}.}

\item{linear_covs}{Optional numeric matrix of covariates that enter the
linear predictor additively (\eqn{\mathbf{z}_{lin}}). \code{NULL} omits.}

\item{emax_covs}{Optional numeric matrix of covariates that modify the Emax
parameter additively (\eqn{\mathbf{z}_{emax}}). \code{NULL} omits.}

\item{ec50_covs}{Optional numeric matrix of covariates that modify
\eqn{\log(EC_{50})} additively (\eqn{\mathbf{z}_{ec50}}); a coefficient
of \eqn{\delta} multiplies EC50 by \eqn{e^\delta} per unit increase.
\code{NULL} omits.}

\item{start}{Starting values. One of:
\itemize{
\item \code{NULL} (default): auto-derived from a simple logistic regression;
covariate effects start at 0.
\item A length-3 vector \code{c(e0, emax, ec50)} on the natural scale:
base parameters; covariate effects start at 0.
\item A full-length vector of length
\code{3 + ncol(linear_covs) + ncol(emax_covs) + ncol(ec50_covs)} ordered
\verb{(e0, emax, ec50, beta..., gamma..., delta...)}, all on the natural
scale (ec50 is log-transformed internally).
}}

\item{control}{A list of control parameters passed to \code{\link[=optim]{optim()}}. Default
\code{list(maxit = 1000)}.}

\item{hessian}{Logical; if \code{TRUE} (default) the Hessian is computed and used
to derive the variance-covariance matrix and standard errors.}

\item{...}{Additional arguments passed to \code{\link[=optim]{optim()}}.}
}
\value{
An S3 object of class \code{"logistic_emax"}, a named list with:
\describe{
\item{\code{coefficients}}{Named numeric vector on the reporting scale:
\verb{c(e0, emax, ec50, linear.*, emax.*, log_ec50.*)}.  \code{ec50} is
back-transformed; EC50 covariate effects (\verb{log_ec50.*}) remain on the
log scale.}
\item{\code{std_err}}{Standard errors matching \code{coefficients} (delta method
applied to \code{ec50}).}
\item{\code{vcov_theta}}{Variance-covariance matrix in the optimisation
parameterisation \verb{(e0, emax0, log_ec50_0, beta..., gamma..., delta...)}, or \code{NULL} when unavailable.}
\item{\code{theta_hat}}{Raw optimised parameter vector (same parameterisation
as \code{vcov_theta}).}
\item{\code{fitted.values}}{Fitted probabilities for the observed data.}
\item{\code{linear.predictors}}{Fitted values on the logit scale.}
\item{\code{residuals}}{Raw residuals \code{y - fitted.values}.}
\item{\code{loglik}}{Log-likelihood at the MLE.}
\item{\code{AIC}}{Akaike information criterion.}
\item{\code{converged}}{\code{TRUE} when \code{\link[=optim]{optim()}} converged.}
\item{\code{n_par}}{Total number of parameters.}
\item{\code{x}, \code{y}}{Complete-case predictor and outcome vectors.}
\item{\code{Z_lin}, \code{Z_emax}, \code{Z_ec50}}{Covariate matrices used in fitting
(0-column matrices when the corresponding argument was \code{NULL}).}
\item{\code{idx}}{Named list of integer indices into \code{theta_hat} for each
parameter group, used internally by \code{\link[=predict.logistic_emax]{predict.logistic_emax()}}.}
\item{\code{call}}{The matched function call.}
}
}
\description{
Fits a logistic Emax model to binary outcome data via maximum likelihood,
with optional additive linear covariates and covariate effects on the Emax
and EC50 parameters.
}
\details{
The model for observation \eqn{i} is
\deqn{
  \eta_i = E_0
    + \bigl(E_{max} + \mathbf{z}_{emax,i}^\top\boldsymbol{\gamma}\bigr)
      \frac{x_i}{e^{\ell_i} + x_i}
    + \mathbf{z}_{lin,i}^\top\boldsymbol{\beta},
}
where \eqn{\ell_i = \log(EC_{50}) + \mathbf{z}_{ec50,i}^\top\boldsymbol{\delta}}
keeps \eqn{EC_{50,i} > 0}, and
\eqn{P(Y_i=1) = \text{logistic}(\eta_i)}.
When all covariate arguments are \code{NULL} the model reduces to the standard
three-parameter logistic Emax formula.
}
\section{Covariate matrices}{

\code{linear_covs}, \code{emax_covs}, and \code{ec50_covs} must be numeric matrices (or
objects coercible via \code{\link[=as.matrix]{as.matrix()}}) with \code{length(y)} rows.  Expand factors
in advance with \code{\link[=model.matrix]{model.matrix()}}.  Column names label the coefficients;
unnamed columns receive labels \code{"V1"}, \code{"V2"}, etc.
}

\examples{
set.seed(42)
n    <- 200
dose <- sort(rep(c(0, 5, 10, 25, 50, 100), length.out = n))
eta  <- qlogis(0.10) + (qlogis(0.75) - qlogis(0.10)) * dose / (10 + dose)
y    <- rbinom(n, 1, plogis(eta))

# Basic model
fit <- fit_logistic_emax(y, dose)
print(fit)

# Linear covariate on the link scale
wt  <- rnorm(n, 70, 10)
fit2 <- fit_logistic_emax(y, dose, linear_covs = cbind(weight = wt))
print(fit2)

# Covariate modifying Emax
grp <- rbinom(n, 1, 0.5)
fit3 <- fit_logistic_emax(y, dose, emax_covs = cbind(group = grp))
print(fit3)

}
\seealso{
\code{\link[=predict.logistic_emax]{predict.logistic_emax()}}, \code{\link[=print.logistic_emax]{print.logistic_emax()}},
\code{\link[=roc_logistic_emax]{roc_logistic_emax()}}
}
