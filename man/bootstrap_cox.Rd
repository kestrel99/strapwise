% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_cox.R
\name{bootstrap_cox}
\alias{bootstrap_cox}
\title{Bootstrap a fitted Cox proportional hazards model}
\usage{
bootstrap_cox(fit, n_boot = 1000L, seed = 42L, conf_level = 0.95)
}
\arguments{
\item{fit}{A fitted \code{\link[survival:coxph]{survival::coxph()}} object.}

\item{n_boot}{Number of bootstrap replicates. Default \code{1000L}.}

\item{seed}{Integer random seed passed to \code{\link[=set.seed]{set.seed()}}. Default \code{42L}.}

\item{conf_level}{Confidence level for percentile intervals, strictly
between 0 and 1. Default \code{0.95}.}
}
\value{
An S3 object of class \code{"boot_cox"}, a named list with:
\describe{
\item{\code{fit}}{The original \code{coxph} object.}
\item{\code{boot_coefs}}{Numeric matrix of bootstrapped log hazard-ratio
estimates (\code{n_success} x \code{p}), after dropping incomplete rows.}
\item{\code{data}}{The original data frame recovered from the fit call.}
\item{\code{formula}}{The model formula.}
\item{\code{conf_level}}{The requested confidence level.}
\item{\code{n_boot}}{Number of successful bootstrap replicates retained.}
}
}
\description{
Runs case-resampling bootstrap on a fitted \code{\link[survival:coxph]{survival::coxph()}} object,
returning a matrix of bootstrapped log hazard-ratio estimates alongside the
original fit. The result can be inspected via \code{\link[=print.boot_cox]{print.boot_cox()}} and
\code{\link[=summary.boot_cox]{summary.boot_cox()}}.
}
\details{
The function recovers the original data from \code{fit$call$data} so that the
full (untransformed) data frame is available for row resampling. If the call
environment cannot be evaluated it falls back to \code{model.frame(fit)} with a
warning. The \code{ties} method of the original fit is preserved in every
bootstrap refit.

Replicates that error or produce warnings during refitting (e.g. Firth
correction triggers, collinearity warnings) are skipped and counted. If any
are skipped a message is emitted. Replicates with any \code{NA} coefficient are
dropped before the result is returned.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  lung <- survival::lung
  lung$status <- lung$status - 1L   # recode to 0/1
  fit <- survival::coxph(
    survival::Surv(time, status) ~ age + sex + ph.ecog,
    data = lung
  )
  bc <- bootstrap_cox(fit, n_boot = 200, seed = 1)
  print(bc)
  summary(bc)
}

}
\seealso{
\code{\link[=summary.boot_cox]{summary.boot_cox()}}, \code{\link[=print.boot_cox]{print.boot_cox()}}
}
