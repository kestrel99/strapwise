% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_emax.R
\name{bootstrap_emax}
\alias{bootstrap_emax}
\title{Bootstrap a fitted logistic Emax model}
\usage{
bootstrap_emax(fit, n_boot = 1000L, seed = 42L, conf_level = 0.95)
}
\arguments{
\item{fit}{A fitted \code{\link[=fit_logistic_emax]{fit_logistic_emax()}} object.}

\item{n_boot}{Number of bootstrap replicates. Default \code{1000L}.}

\item{seed}{Integer random seed passed to \code{\link[=set.seed]{set.seed()}}. Default \code{42L}.}

\item{conf_level}{Confidence level for percentile intervals, strictly
between 0 and 1. Default \code{0.95}.}
}
\value{
An S3 object of class \code{"boot_emax"}, a named list with:
\describe{
\item{\code{fit}}{The original \code{logistic_emax} object.}
\item{\code{boot_theta}}{Numeric matrix of bootstrapped parameter vectors
(\code{n_success} x \code{n_par}) in the optimisation parameterisation
\verb{(e0, emax, log_ec50, ...)}  -- the same scale as \code{fit$theta_hat}.}
\item{\code{conf_level}}{The requested confidence level.}
\item{\code{n_boot}}{Number of successful bootstrap replicates retained.}
}
}
\description{
Runs case-resampling bootstrap on a fitted \code{logistic_emax} object, returning
a matrix of bootstrapped parameter vectors alongside the original fit. The
result can be inspected via \code{\link[=print.boot_emax]{print.boot_emax()}} and \code{\link[=summary.boot_emax]{summary.boot_emax()}}.
}
\details{
Each replicate resamples the rows of the original data (including all
covariate matrices) with replacement and refits \code{\link[=fit_logistic_emax]{fit_logistic_emax()}} using
the original estimates as warm-start values. Replicates that error, produce
warnings, or fail to converge are skipped and counted. A message is emitted
if any are skipped.
}
\examples{
set.seed(42)
n    <- 200
dose <- sort(rep(c(0, 5, 10, 25, 50, 100), length.out = n))
eta  <- qlogis(0.10) + (qlogis(0.75) - qlogis(0.10)) * dose / (10 + dose)
y    <- rbinom(n, 1, plogis(eta))

fit <- fit_logistic_emax(y, dose)
be  <- bootstrap_emax(fit, n_boot = 200, seed = 1)
print(be)
summary(be)

}
\seealso{
\code{\link[=fit_logistic_emax]{fit_logistic_emax()}}, \code{\link[=summary.boot_emax]{summary.boot_emax()}}
}
